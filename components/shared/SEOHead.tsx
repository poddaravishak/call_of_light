/**
 * Placeholder — in the App Router, SEO is handled via `generateMetadata`
 * exports in route files. This component is kept only to match the plan's
 * structure in case any client-only head injection is later needed.
 */
export function SEOHead() {
  return null;
}
